const config = {
    // 启动端口
    port: 3000,

    // 数据库链接配置
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: 'gaowei',
        PORT: '3306',
        HOST: '127.0.0.1'
    }

}

module.exports = config